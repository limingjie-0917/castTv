<?xml version="1.0" encoding="utf-8"?>
<!--
    全局「系统提示气泡」（生活作息 + 健康提醒）overlay 布局：
    - 通过 Activity.addContentView 挂到 android.R.id.content，顶部居中、距顶留边距（代码设置）；
    - 玻璃拟态背景 + 高 elevation 外阴影，呈现系统横幅质感；
    - 根容器不可聚焦（绝不参与 D-pad 焦点分发、不干扰遥控器导航）；
    - 仅「关闭按钮」可聚焦/可点击：手机触摸即关，TV 遥控器可移动焦点到它按 OK 关闭；
    - 主文案随时间段与状态（常规/健康提醒）由代码填充；已按用户要求移除左侧图标与二级小字。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/routineBubbleRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_routine_bubble"
    android:clickable="false"
    android:elevation="20dp"
    android:focusable="false"
    android:focusableInTouchMode="false"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingLeft="20dp"
    android:paddingTop="12dp"
    android:paddingRight="12dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/routineBubbleMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:focusable="false"
        android:letterSpacing="0.01"
        android:maxWidth="420dp"
        android:maxLines="1"
        android:text="@string/routine_bubble_placeholder_main"
        android:textColor="#FF1F2937"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/routineBubbleClose"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="14dp"
        android:background="@drawable/bg_routine_close"
        android:clickable="true"
        android:contentDescription="@string/routine_bubble_close"
        android:focusable="true"
        android:gravity="center"
        android:text="✕"
        android:textColor="#B01F2937"
        android:textSize="15sp" />
</LinearLayout>
