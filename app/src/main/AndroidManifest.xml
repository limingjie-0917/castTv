<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Networking: HTTP server, SSDP multicast discovery -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Keep the DLNA renderer alive in the background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Force-launch the player from background / lockscreen when a cast arrives -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 本地文件访问：收藏数据持久化。实际存储位于 App 私有目录（filesDir），
         这两条声明用于在低版本系统上申请本地文件读写权限（Android 10+ 已启用分区存储，无需该权限）。 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Android TV: no touchscreen, leanback launcher -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <application
        android:name=".CastApp"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CastTV"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Splash screen. Registered as launcher entry and jumps to MainActivity after 2 seconds. -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CastTV.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Waiting / status screen. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.CastTV.Launch" />

        <!-- New architecture main Activity; internal test entry only, not launcher yet. -->
        <activity
            android:name=".ui.framework.NewMainActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CastTV.Fullscreen" />

        <!-- New architecture skeleton demo; internal test entry only. -->
        <activity
            android:name=".ui.framework.demo.FrameworkDemoActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CastTV.Fullscreen" />

        <!-- Full-screen player launched when a cast arrives -->
        <activity
            android:name=".player.PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.CastTV.Fullscreen" />

        <!-- Foreground service started on boot to quietly keep DLNA/AirPlay listeners alive. -->
        <service
            android:name=".receiver.CastReceiverService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Foreground service hosting SSDP + HTTP (the DLNA renderer core) -->
        <service
            android:name=".dlna.DlnaRendererService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Foreground service hosting the AirPlay 1 mirroring receiver
             (mDNS _airplay._tcp/_raop._tcp + Netty control/mirror servers).
             Runs in parallel with the DLNA renderer. -->
        <service
            android:name=".airplay.AirPlayService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Android TV DreamService：系统屏保海报墙。 -->
        <service
            android:name=".douyin.DouyinDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service
            android:name=".screensaver.PosterDreamService"
            android:exported="true"
            android:permission="android.permission.BIND_DREAM_SERVICE">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
            </intent-filter>
        </service>

        <!-- App 内海报墙：前台空闲（5 分钟无交互）时由 MainActivity 触发拉起。 -->
        <activity
            android:name=".screensaver.PosterWallActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.CastTv.PosterWall"
            android:configChanges="keyboardHidden|orientation|screenSize|uiMode" />

        <!-- App 内更新下载的 APK 通过 FileProvider 授权给系统安装器读取。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
