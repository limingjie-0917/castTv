本报告只做技术调研与可行性评估，不改动任何代码。评估对象为工程 `casttv-receiver` 现有 DLNA DMR（MediaRenderer）接收端实现，逐项核对 P0 五个方向的代码现状、缺口，对照 UPnP/DLNA 与 ExoPlayer 的通用实践，给出可行性与改动量判断。

<callout icon="bulb" bgc="3">  
- **整体结论**：五项 P0 全部可行，且都在现有自研 DLNA 基座上增量完成，无需引入商业投屏 SDK。  
- **兼容性（P0-1）已基本达标**：华为/小米/OPPO 视频 App 内投屏均走标准 DLNA，当前 SSDP + MediaRenderer 描述已可被发现；剩余为 `manufacturer/modelName` 占位符等小幅完善。  
- **状态同步（P0-2）主链路已通**：`GetTransportInfo`/`GetPositionInfo` 已正确响应，主流发送端靠 2-3 秒轮询即可显示进度；GENA 事件推送缺失只影响少数依赖事件的发送端，属可选增强。  
- **最高优先级是稳定性（P0-3）与兜底（P0-4）**：重连只做 1 次且无退避、相同 URI 不去重会导致重复起播，空地址/DRM 内容会黑屏无提示——这两项直接对应用户反馈的"重复几次才成功""投屏没反应"，改动集中、收益最大。  
- **格式声明（P0-5）需补 H.265**：`SinkProtocolInfo` 未显式声明 HEVC/TS/WebM，部分做能力协商的发送端会因此不提供高清/某些格式，补齐常量即可，但真正 4K/H.265 播放仍受电视芯片与 DRM 限制。  
</callout>

## 调研范围与现状基线

评估以下核心代码路径的实际实现，而非文档描述：

- **发现层**：`dlna/SsdpService.kt`（SSDP 组播、M-SEARCH 应答、alive 通告）
- **描述与控制层**：`dlna/UpnpXml.kt`（设备描述 XML、SCPD、SOAP 封装）、`dlna/DlnaHttpServer.kt`（NanoHTTPD 路由、SOAP 动作、GENA）
- **状态桥接层**：`dlna/PlaybackController.kt`（SOAP 线程与播放器状态互通）
- **播放层**：`player/PlayerActivity.kt`（ExoPlayer 起播、重连、错误处理）

一句话基线：**标准 UPnP AV MediaRenderer:1 的发现、描述、SOAP 控制、ExoPlayer 播放主链路已经跑通**，本轮 P0 是在这个可用基座上补齐兼容边界、稳定性和异常兜底。以下每项的"现状"均来自上述代码的直接核对，"业界实践"来自 UPnP 规范与公开工程讨论。

## P0-1 DLNA 兼容性：兼容华为/小米/OPPO 系统投屏

**现状（已具备）**。`SsdpService` 加入 `239.255.255.250:1900`，`matchTargets` 对 `upnp:rootdevice`、`MediaRenderer:1`、三个 service 类型、设备 UDN 以及 `ssdp:all` 均能应答；M-SEARCH 单播响应按 `RESPONSE_REPEATS=3` 错峰重发、启动时 `ALIVE_BURST_ROUNDS=3` 连发 alive 抗丢包。`UpnpXml.buildDeviceDescription` 输出标准 `MediaRenderer:1` 描述，含 `X_DLNADOC=DMR-1.50` 与 AVTransport/RenderingControl/ConnectionManager 三服务。

**关键事实：三家手机的"应用内投屏"走的是标准 DLNA**。华为/小米/OPPO/vivo 在爱奇艺、腾讯视频、B 站等 App 内点击投屏图标时，使用的是通用 DLNA/UPnP 协议，因此当前实现即可被发现和控制。需要区分两类无法用标准 DLNA 覆盖的场景：一是系统级"手机镜像/无线投屏"走 Miracast（Wi-Fi P2P，与 DLNA 不同栈，不在接收端范围）；二是华为 Cast+、小米私有互联等**私有协议需要厂商授权**，非标准 DLNA 能对接。

**缺口（均为小幅完善）**：

- `manufacturer` 为 `CastTV`、`manufacturerURL` 为 `https://example.com`、`modelName` 为 `CastTV Receiver`（`UpnpXml.kt:44-47`）。极少数发送端会用厂商/型号做展示或白名单匹配，占位符不影响发现，但建议改为真实可辨识值。
- 仅监听 IPv4 组播，未处理 IPv6（`FF0x::C`）。国内家庭 Wi-Fi 基本为 IPv4，优先级低。

**可行性：高，改动量小**。核心兼容能力已到位，剩余是描述文件字段完善，属文本级修改。私有协议与 Miracast 镜像明确列为非目标。

## P0-2 播放状态同步：SOAP 查询接口正确响应

**现状（主链路已通）**。`DlnaHttpServer` 已实现 `GetTransportInfo`（返回 `CurrentTransportState` + `Status=OK` + `Speed=1`）与 `GetPositionInfo`（返回 `TrackDuration`/`RelTime`/`AbsTime`/`TrackURI`），数据来自 `PlaybackController` 中由播放器实时回写的 `positionMs`/`durationMs`；`PlayerActivity` 在播放中每 1 秒调用 `updateProgress` 刷新。`GetMediaInfo`、`GetCurrentTransportActions` 等配套动作也已实现。

**业界实践支撑当前方案**。UPnP 控制点的通用做法是每 2-3 秒轮询 `GetPositionInfo`，把返回的相对时间作为本地计时器的校准值（见 StackOverflow "UPNP/DLNA Control Point" 讨论）。也就是说，进度条同步的主责任在发送端轮询，接收端只要把查询答对即可——这一点当前已满足。

**缺口（可选增强）**。`DlnaHttpServer` 对 GENA `SUBSCRIBE` 只回了带 SID 的 200 OK，**从不主动发送 `NOTIFY`/`LastChange` 事件**。少数依赖事件推送而非轮询的发送端，进度/状态不会被主动推更新。考虑到很多成熟渲染器也只在状态切换（play/pause/seek）时发一次 `LastChange`，且中文主流 App 以轮询为主，此项定位为"锦上添花"。

**可行性：满足主流为高；完整 GENA 事件为中等工作量**。若要覆盖依赖事件的发送端，需实现 AVTransport `LastChange` 事件构造 + 订阅者管理 + 定时/状态变更推送，改动集中在 `DlnaHttpServer` 与 `PlaybackController`。

## P0-3 播放稳定性：抖动不卡顿/断流/重复起播

这是与用户反馈"重复几次才能投屏成功"最直接相关的一项，也是本轮收益最高的方向。

**现状**。`PlayerActivity.initPlayer` 使用自定义 `DefaultLoadControl`：`minBuffer=15s / maxBuffer=50s / bufferForPlayback=700ms / afterRebuffer=1.5s`，并 `setPrioritizeTimeOverSizeThresholds(true)` 以加快起播；`DefaultHttpDataSource` 连接/读取超时各 8 秒，允许跨协议重定向。

**缺口一：重连过于保守（对应"断流"）**。`tryReconnect()`（`PlayerActivity.kt:275`）在网络类错误时只调用一次 `prepare()`，**无重试次数、无退避延迟**；若这一次仍未恢复，随即 `scheduleStreamEndTimer` 在超时后退回待机。瞬时 Wi-Fi 抖动很容易一次重连不成功即放弃。ExoPlayer 的通用做法是在网络错误后按有限次数（如 3 次）配合递增延迟重复 `prepare()` 重试。

**缺口二：相同 URI 不去重（对应"重复起播"）**。`commandCallback.onSetUri`（`PlayerActivity.kt:286`）无条件 `setMediaItem` + `prepare` + `playWhenReady=true`。部分发送端会重复下发相同的 `SetAVTransportURI`（或"发现即预连接 + 正式投屏"两次下发），当前实现会把正在播放的同一地址重新加载，表现为画面闪断/重复起播。建议对"URI 未变且正在播放"直接忽略。

**缺口三：`STATE_IDLE` 未触发自愈**。进入 `STATE_IDLE` 仅上报 STOPPED，未尝试恢复。

**可行性：高，改动集中**。三点修改都落在 `PlayerActivity` 与 `PlaybackController`：重连加"次数 + 退避"、`onSetUri` 加 URI 去重、必要时对 `IDLE` 做有限自愈。缓冲参数可保持"快起播"，仅在需要时把抖动容忍略微上调，属参数级调整。

## P0-4 失败兜底：友好提示，不黑屏/崩溃

**现状**。`onPlayerError`（`PlayerActivity.kt:254`）已对错误码分类：解码/格式类 → `player_error_decode` 文案；网络类 → 触发重连并显示 `player_reconnecting`；其他 → 通用错误码文案。有居中状态浮层覆盖，不会崩溃，超时后退回待机。

**缺口一：空地址黑屏**。`initPlayer` 在 `currentUri` 为空时直接跳过 `prepare()`（`PlayerActivity.kt:193`），既不播放也**不显示任何提示**，用户看到的是黑屏。应对空/非法地址给出"未获取到有效播放地址"。

**缺口二：DRM 内容无专门提示**。未处理 `ERROR_CODE_DRM_*`，此类会落到通用文案。爱奇艺/腾讯视频等高清内容多为 Widevine DRM 保护，投屏此类内容必然失败，需要明确提示"该内容受版权保护，无法投屏"，避免用户误以为是软件故障。这一点与 P0-5 的"4K/高清受限"同源。

**可行性：高，工作量低**。纯 UI 文案 + 错误分支补充，新增 DRM/空地址两类友好提示即可，无架构改动。

## P0-5 格式兼容：H.265/4K 与描述文件声明

**现状**。ExoPlayer（media3）通过 `DefaultMediaSourceFactory` 自动识别 Progressive/HLS/DASH，实际解码能力取决于电视芯片。`GetProtocolInfo` 返回的 `SinkProtocolInfo`（`DlnaHttpServer.kt:307`）当前如下：

<table header-row="true" header-col="false" col-widths="220,300,300">  
  <tr>  
    <td>维度</td>  
    <td>现状声明</td>  
    <td>缺口 / 建议补充</td>  
  </tr>  
  <tr>  
    <td>通配</td>  
    <td>`http-get:*:*:*`、`video/*`、`audio/*`、`image/*`</td>  
    <td>已有，理论上"全接受"</td>  
  </tr>  
  <tr>  
    <td>视频容器/编码</td>  
    <td>`video/mp4`、`video/x-matroska`、`video/mpeg`、`video/avi`、`video/quicktime`</td>  
    <td>缺 `video/hevc`、`video/H265`、`video/mp2t`(TS)、`video/webm`、`video/3gpp`</td>  
  </tr>  
  <tr>  
    <td>自适应流</td>  
    <td>HLS(`application/vnd.apple.mpegurl`、`x-mpegURL`)、DASH(`application/dash+xml`)</td>  
    <td>已覆盖</td>  
  </tr>  
  <tr>  
    <td>DLNA 能力标记</td>  
    <td>无 `DLNA.ORG_OP`/`DLNA.ORG_FLAGS`</td>  
    <td>可加 `DLNA.ORG_OP=01`（支持 seek）等，供发送端判断能否拖动</td>  
  </tr>  
</table>

**缺口分析**。虽然存在 `http-get:*:*:*` 通配，理论上任何格式都被接受，但部分做能力协商的发送端会**忽略通配、按显式 MIME 或 DLNA profile 判断**是否提供投屏或某清晰度选项。公开案例（如 Jellyfin issue #8175：即便 TV 支持 HEVC，服务器因 profile 声明不当仍强制转码）说明 `protocolInfo`/profile 声明的正确性会直接影响播放路径。因此补齐 HEVC/TS/WebM 等显式条目对"能不能出现 H.265/高清选项"有实际意义。

**必须说明的边界**：`SinkProtocolInfo` 只是"声明层"，真正能否播放 4K/H.265 取决于电视 SoC 的硬解能力；而受 DRM 保护的高清内容无论如何声明都无法投屏（与 P0-4 关联）。

**可行性：高，工作量低**。仅需扩充 `SINK_PROTOCOL_INFO` 常量并可选加 DLNA.ORG 标记，无逻辑改动。

## 综合可行性与优先级

<table header-row="true" header-col="false" col-widths="70,220,180,90,110">  
  <tr>  
    <td>项</td>  
    <td>核心缺口</td>  
    <td>改动面</td>  
    <td>工作量</td>  
    <td>建议优先级</td>  
  </tr>  
  <tr>  
    <td>P0-3</td>  
    <td>重连无退避/无重试次数、相同 URI 不去重、IDLE 不自愈</td>  
    <td>`PlayerActivity`、`PlaybackController`</td>  
    <td>中</td>  
    <td><font color="red">**最高**</font></td>  
  </tr>  
  <tr>  
    <td>P0-4</td>  
    <td>空地址黑屏、DRM 无专门提示</td>  
    <td>`PlayerActivity` + `strings.xml`</td>  
    <td>低</td>  
    <td><font color="red">**最高**</font></td>  
  </tr>  
  <tr>  
    <td>P0-5</td>  
    <td>`SinkProtocolInfo` 缺 HEVC/TS/WebM 及 DLNA 标记</td>  
    <td>`DlnaHttpServer` 常量</td>  
    <td>低</td>  
    <td>高</td>  
  </tr>  
  <tr>  
    <td>P0-1</td>  
    <td>厂商/型号占位符；IPv6 未支持</td>  
    <td>`UpnpXml` 描述文本</td>  
    <td>低</td>  
    <td>中</td>  
  </tr>  
  <tr>  
    <td>P0-2</td>  
    <td>无 GENA `LastChange` 事件推送（主流靠轮询已满足）</td>  
    <td>`DlnaHttpServer`、`PlaybackController`</td>  
    <td>中</td>  
    <td>中（可选增强）</td>  
  </tr>  
</table>

判断依据：P0-3/P0-4 直接对应用户已反馈的失败体验且改动集中，应先做；P0-5/P0-1 是低成本高确定性的兼容完善；P0-2 的事件推送因主流发送端走轮询，收益有限，可按需排后。

## 实施建议与验证

建议按"稳定性与兜底优先，再做声明完善，最后视需要补事件"的顺序推进。落地时的验证与验收：

- [ ] **P0-3 重连**：`tryReconnect` 引入最多 3 次、递增延迟（如 1s/2s/4s）的 `prepare()` 重试，全部失败才退待机；`onSetUri` 对"URI 未变且正在播放"直接忽略。验收：断开 Wi-Fi 3-5 秒再恢复可自动续播；同一视频重复点投屏不闪断。
- [ ] **P0-4 兜底**：空/非法地址显示"未获取到有效播放地址"；新增 `ERROR_CODE_DRM_*` 分支提示"内容受版权保护无法投屏"。验收：投空地址、投 DRM 内容均有明确文案，无黑屏无崩溃。
- [ ] **P0-5 声明**：`SINK_PROTOCOL_INFO` 增加 `video/hevc`、`video/H265`、`video/mp2t`、`video/webm`、`video/3gpp`，可选加 `DLNA.ORG_OP=01`。验收：具备 H.265 硬解的电视上，H.265 片源可被发送端提供并播放。
- [ ] **P0-1 描述**：`manufacturer`/`modelName`/`manufacturerURL` 改为真实可辨识值。验收：华为/小米/OPPO 视频 App 内可稳定发现并显示设备名。
- [ ] **P0-2 事件（可选）**：实现 AVTransport `LastChange` 事件与订阅推送。验收：使用依赖 GENA 事件的控制点时进度/状态可被主动更新。

真机验证建议覆盖：小米/华为/OPPO 各一台手机 × 爱奇艺/腾讯/B 站 App，记录"发现成功率、起播耗时、抖动恢复、异常提示"四项。

## 风险与非目标

<table header-row="true" header-col="false" col-widths="240,420">  
  <tr>  
    <td>事项</td>  
    <td>说明</td>  
  </tr>  
  <tr>  
    <td>DRM 高清内容无法投屏</td>  
    <td>爱奇艺/腾讯等 Widevine 保护内容，任何标准 DLNA 接收端都无法解密播放；P0-4 只能做到"友好提示"，非"支持播放"。</td>  
  </tr>  
  <tr>  
    <td>系统镜像投屏不在范围</td>  
    <td>手机"无线投屏/镜像"走 Miracast（Wi-Fi P2P），与 DLNA 接收端不同栈，本轮不覆盖。</td>  
  </tr>  
  <tr>  
    <td>私有互联协议</td>  
    <td>华为 Cast+、小米私有互联需厂商授权，标准 DLNA 无法对接；如需覆盖须评估商业 SDK。</td>  
  </tr>  
  <tr>  
    <td>4K/H.265 实际播放受硬件限制</td>  
    <td>P0-5 仅解决"声明层"，能否流畅播放取决于电视 SoC 硬解能力，低端芯片仍可能卡顿或不支持。</td>  
  </tr>  
</table>

📌 待确认：本轮是否需要一并实现 P0-2 的 GENA 事件推送，还是先只做轮询满足主流、把事件推送放到后续迭代。这决定 P0-2 的排期与工作量。
